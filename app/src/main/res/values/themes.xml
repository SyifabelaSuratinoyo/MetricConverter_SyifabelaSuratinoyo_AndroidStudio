<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MetricConverter" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Menjaga ikon status bar tetap terlihat jika background terang -->
    </style>

    <style name="Theme.MetricConverter" parent="Base.Theme.MetricConverter" />
</resources>
